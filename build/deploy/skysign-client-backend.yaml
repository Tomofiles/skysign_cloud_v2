apiVersion: apps/v1
kind: Deployment
metadata:
  name: skysign-client-backend
  labels:
    run: skysign-client-backend
spec:
  selector:
    matchLabels:
      run: skysign-client-backend
  replicas: 1
  template:
    metadata:
      labels:
        run: skysign-client-backend
    spec:
      containers:
      - name: skysign-client-backend
        image: gcr.io/${GOOGLE_PROJECT_ID}/client:${CIRCLE_SHA1}
        ports:
        - containerPort: 5000