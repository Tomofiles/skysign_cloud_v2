apiVersion: v1
kind: Service
metadata:
  name: skysign-flightplan
  labels:
    run: skysign-flightplan
spec:
  clusterIP: None
  ports:
  - port: 5001
    protocol: TCP
  selector:
    run: skysign-flightplan-backend