apiVersion: apps/v1
kind: Deployment
metadata:
  name: skysign-helper-gateway-backend
  labels:
    run: skysign-helper-gateway-backend
spec:
  selector:
    matchLabels:
      run: skysign-helper-gateway-backend
  replicas: 1
  template:
    metadata:
      labels:
        run: skysign-helper-gateway-backend
    spec:
      containers:
      - name: skysign-helper-gateway-backend
        image: gcr.io/${GOOGLE_PROJECT_ID}/http_gateway:${CIRCLE_SHA1}
        command: ["/app/helper-gateway"]
        args: ["-port", "6000", "-backend_host", "skysign-helper", "-backend_port", "5001"]
        ports:
        - containerPort: 6000