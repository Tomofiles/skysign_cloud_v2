apiVersion: apps/v1
kind: Deployment
metadata:
  name: skysign-http-gateway-vehicle-backend
  labels:
    run: skysign-http-gateway-vehicle-backend
spec:
  selector:
    matchLabels:
      run: skysign-http-gateway-vehicle-backend
  replicas: 1
  template:
    metadata:
      labels:
        run: skysign-http-gateway-vehicle-backend
    spec:
      containers:
      - name: skysign-http-gateway-vehicle-backend
        image: gcr.io/${GOOGLE_PROJECT_ID}/http_gateway:${CIRCLE_SHA1}
        command: ["/app/comm-vehicle-gateway"]
        args: ["-port", "8889", "-backend_host", "skysign-communication", "-backend_port", "5001"]
        ports:
        - containerPort: 8889